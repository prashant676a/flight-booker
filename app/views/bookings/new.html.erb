<h2>Booking Page</h2>

Flight From <%= @flight.departure_airport.code %> to <%= @flight.arrival_airport.code%> <br>
Date <%= @flight.start_datetime.strftime("%d %b, %Y")%>
Flight Id <%= @flight.id %>

<h3>Personal Information </h3>

<%= form_for @booking do |f| %>
  <%= f.fields_for :passengers do |passenger_builder|%>
    <%= passenger_builder.label :name %>
    <%= passenger_builder.text_field :name %> <br><br>
    <%= passenger_builder.label :email %>
    <%= passenger_builder.text_field :email %>
  <% end %>
  <br><br>
  <%= f.submit %>
<% end %>